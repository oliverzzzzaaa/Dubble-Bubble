// function Keybindings(game) {
//     function onkeydown(e) {
//         // if (e.key == ' ' || e.key == 'Enter') { downKeys.fire = true };
//         // if (e.key == 'w' || e.key == 'ArrowUp') { downKeys.up = true };
//         // if (e.key == 's' || e.key == 'ArrowDown') { downKeys.down = true };
//         if (e.key == 'a' || e.key == 'ArrowLeft') { game.map.players[0].vel = [-10, 0] };
//         if (e.key == 'd' || e.key == 'ArrowRight') { game.map.players[0].vel = [10, 0] };
// };
//     function onkeyup(e){
//     // if (e.key == ' ' || e.key == 'Enter') {downKeys.fire = false};
//     // if (e.key == 'w' || e.key == 'ArrowUp') {downKeys.up = false};
//     // if (e.key == 's' || e.key == 'ArrowDown') {downKeys.down = false};
//     if (e.key == 'a' || e.key == 'ArrowLeft') { game.map.players[0].vel = [0, 0] };
//     if (e.key == 'd' || e.key == 'ArrowRight') { game.map.players[0].vel = [0, 0] };
// };

// document.addEventListener('keydown', onkeydown);
// document.addEventListener('keyup', onkeyup);
// }

// Keybindings.prototype.bindKeys = function() {

// }


// module.exports = keybindings;